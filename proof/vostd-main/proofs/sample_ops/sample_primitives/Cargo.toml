[package]
name = "sample_primitives"
version = "0.1.0"
description = "Verification of Sample Primitive code"
edition = "2024"

[lib]
crate-type = ["rlib"]
name = "sample_primitives"
# for cargo
# path = "src/.dummy.rs"
path = "src/lib.rs"

[features]
verus = []
verify = ["verus"]
alloc = []
default = ["verify", "alloc"]

[package.metadata.verus]
path = "src/lib.rs"
verify = true
check_lifetime = true

[dependencies]
sample = { path = "../../code/sample" }
sample_specs = { path = "../../specs/sample" }
vstd = { path = "../../tools/verus/source/vstd" }
verus_builtin = { path = "../../tools/verus/source/builtin" }
verus_builtin_macros = { path = "../../tools/verus/source/builtin_macros" }
