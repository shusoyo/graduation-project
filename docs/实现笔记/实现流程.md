**1. 现状梳理与边界分析**

1. 系统梳理 reL4 中 capability 相关代码当前分布在哪些层：`sel4_cspace`、`cnode syscall invocation`、`untyped invocation`。
2. 列出每个操作当前由谁负责：`Copy`、`Mint`、`Move`、`Mutate`、`Rotate`、`Delete`、`Revoke`、`Retype`。
3. 给出一张“职责矩阵”：哪些属于 CSpace 核心原语，哪些属于 syscall 编排，哪些属于 untyped/对象创建。
4. 分析当前结构为什么不利于验证：边界不稳定、同一语义分散、抽象状态难统一。
5. 形成一份“重构前结构分析”文档，这是你论文前半部分的重要基础。

**2. `sel4_cspace` 的组件化重构**

1. 设计重构后的模块边界，明确 `sel4_cspace` 对外应该提供哪些原语接口。
2. 把适合下沉到 CSpace 的 capability 操作逻辑，从 syscall invocation 中进一步抽到 `sel4_cspace`。
3. 保留 syscall 层的职责为：参数解析、合法性检查、错误码转换、线程状态切换、调用原语。
4. 保留 untyped 层的职责为：对象创建、资源划分、untyped 状态更新；只把其中与 cap 插入/派生树维护直接相关的部分纳入 cspace 视角。
5. 完成重构后，要给出“重构前/后对比”：代码边界、接口变化、职责变化。

**3. 抽象模型与不变式设计**

1. 建立 `sel4_cspace` 的抽象状态模型，至少要包含：`Cap`、`Slot`、`Object`、权限、`MDB/CDT` 派生关系。
2. 定义抽象层中的核心关系：slot 中存什么 cap、cap 指向什么对象、父子派生关系如何表示。
3. 给出关键不变式，例如：
   - slot 与 cap 状态一致性；
   - capability 派生关系合法；
   - 删除/撤销后不会留下悬挂关系；
   - 移动/插入不会破坏对象引用关系；
   - 权限衰减与 mint/copy 语义一致。
4. 说明这些不变式分别由哪些原语保持。

**4. 核心操作规约设计**

1. 为 `Copy`、`Mint`、`Move`、`Delete`、`Revoke` 分别写抽象规约，至少包括前置条件、后置条件、状态变化。
2. 对 `Retype` 不做全功能验证，而是只规约其中与 cspace 直接相关的部分：新 cap 的生成、插入、派生关系建立。
3. 明确 `Mutate`、`Rotate` 是不是作为独立验证对象；如果时间有限，可以把它们降级为“由更基本原语组合实现/说明”。
4. 把“操作规约”和“不变式保持”对应起来，形成后续 Verus 证明目标。

**5. concrete/abstract view 与 Verus 模块级验证**

1. 建立 concrete state 到 abstract state 的 `view`，说明实现里的 `cte/cap/mdb` 如何映射到抽象模型。
2. 选取一组代表性函数做 Verus 验证，不需要一口气全证完，建议优先做：
   - `cte_insert`
   - `cte_move`
   - `delete_all` 或 `revoke`
   - `derive_cap` 或 `insert_new_cap`
3. 在实现中补充 `requires/ensures/invariant`，并拆出必要的辅助引理。
4. 证明这些实现满足抽象规约，并保持关键不变式。
5. 总结哪些已经证明、哪些只完成规约、哪些仍是后续工作。

**6. 实验、论文与答辩材料**

1. 整理重构成果：代码结构变化、接口变化、职责集中效果。
2. 整理验证成果：验证了哪些函数、证明了哪些性质、还存在哪些边界。
3. 撰写论文：问题背景、现状分析、重构设计、抽象模型、规约、不变式、view、验证结果。
4. 准备答辩 PPT 和 demo，demo 最好同时展示“代码边界重构”和“Verus 证明片段”。



1. 先固定门禁和可信边界：哪些先信任，哪些进验证入口。已完成。
2. 再建抽象模型和全局不变量：`CapSpec / CSpaceState / wf` 这层。已完成。
3. 再写原语规格：`cte_insert / cte_move / cte_swap / resolve_address_bits` 的抽象合同。已完成。
4. 再补小引理，把规格收紧成可复用入口。进行中。
5. 然后给 `src` 建 `view/refinement bridge`，把 concrete `cap/cte/ret` 映射到抽象模型。下一步。
6. 再逐个证明具体函数满足对应 spec。随后。
7. 最后做整体回归、文档收口、已证/未证/TCB 清单。最后一步。


可以。我建议把路线改成一条更明确的“`vostd` 主线、`atmo` 边界纪律辅助”的 6 步方案。核心变化是：

- `refinement_bridge.rs` 不再被视为最终形态，只是过渡脚手架。
- 第 6 步的目标不再是“继续证明现有 wrapper 满足 spec”，而是“把证明逐步推回 [cte.rs](/workspace/rel4_kernel/sel4_cspace/src/cte.rs) 的 exec 函数本体”。

**修订版 6 步**
1. 固定最终目标形态与最小 TCB
- 目标明确改成：`cte_insert / cte_move / cte_swap / resolve_address_bits` 最终尽量写成 Verus 风格的 exec 函数，也就是函数本体自己带 `requires/ensures`。
- 只保留不得不留的 TCB：FFI、极少量 raw pointer/bitfield 转换、平台相关底层操作。
- 原则：优先用小型 `assume_specification` / 小型 `external_body util`，不保留大块函数级 trusted wrapper。

2. 固定抽象语义与可复用入口
- 这一层继续保留你现在的做法，不需要推翻。
- 也就是继续以 [abstract_cspace.rs](/workspace/rel4_kernel/sel4_cspace/specs/abstract_cspace.rs) 和 `specs/cspace_ops/*` 为语义源。
- 要求：每个目标函数都先有稳定的抽象 pre/post 和 packaging lemma，再谈 exec proof。
- 这一步你基本已经完成。

3. 把 bridge 改成“临时过渡层”，不再继续膨胀
- [refinement_bridge.rs](/workspace/rel4_kernel/sel4_cspace/src/refinement_bridge.rs) 的职责改成：
  - 暂时承接 raw 数据到抽象模型的映射
  - 暂时集中管理 unavoidable trusted boundary
  - 为把 proof 推回 [cte.rs](/workspace/rel4_kernel/sel4_cspace/src/cte.rs) 提供中间词汇
- 但从这一刻起，不再把“大而全的 bridge wrapper”当长期目标。
- 也就是说：后续新增工作优先是“删除/下推 trusted wrapper 责任”，不是继续给 bridge 加厚。

4. 给 `cte.rs` 里的目标函数建立 Verus-facing 合同入口
- 从“旁证实现”转成“函数本体逐步 Verus 化”。
- 推荐顺序：
  - 先 `resolve_address_bits`
  - 再 `cte_insert`
  - 再 `cte_move`
  - 最后 `cte_swap`
- 做法上分两层：
  - 先让这些函数附近出现 Verus 风格合同入口
  - 再把 proof 从 wrapper 逐步推回函数 body
- 允许短期过渡：合同还在邻近 wrapper；长期目标：合同直接落到 [cte.rs](/workspace/rel4_kernel/sel4_cspace/src/cte.rs) 同名实现上。

5. 逐函数把“大 wrapper 证明”下压成“小 util 假设 + 直接 exec proof”
- 这是新的核心步骤。
- 对每个函数都按这个顺序收：
  - 先把函数级 `trusted_call_*` 缩成若干更小的 util 假设
  - 再让函数 body 直接消费这些小合同
  - 最后删掉函数级 trusted wrapper
- 风格要求：
  - 像 `vostd` 一样，小函数小合同，proof 贴着代码走
  - 像 `atmo` 一样，把残余 trusted util 集中命名、集中管理
- 理想结果：
  - `resolve_address_bits` 不再依赖 `trusted_call_resolve_address_bits`
  - `cte_insert/move/swap` 不再停留在 `local_heap_transition_at` 这一层作为最终证明出口
  - 它们本体自己就能导出抽象 postcondition

6. 最后收口：裁掉过渡 bridge，留下可审计的最小边界
- 当四个核心函数都能直接给出 Verus 合同后，再回头清理 [refinement_bridge.rs](/workspace/rel4_kernel/sel4_cspace/src/refinement_bridge.rs)。
- 这时只保留两类东西：
  - 仍不可避免的底层 trusted util
  - 少量纯 view/投影函数
- 同时产出最终清单：
  - 已直接验证的 exec 函数
  - 仍保留的 trusted util
  - 可继续下推但本轮不做的边界

**和你原路线的关系**
- 原第 1-4 步基本不变，仍然正确。
- 原第 5 步保留，但它现在被重新定义为“过渡脚手架”，不是长期终点。
- 原第 6 步要改写成：
  - 不是“逐个证明具体函数满足 spec”
  - 而是“逐个把 proof 从 bridge/wrapper 推回 `cte.rs` 函数本体”
- 原第 7 步照旧，只是会更清楚，因为那时 bridge 不会再和最终实现层缠在一起。

**一句话版**
新路线应该是：

`spec-first` 不变，`bridge` 只做过渡，`第 6 步` 的真正目标改成“让 [cte.rs](/workspace/rel4_kernel/sel4_cspace/src/cte.rs) 里的核心函数最终自己成为 Verus exec 函数”。

如果你愿意，我下一步可以直接把这个“修订版 6 步”整理成可落到文档里的版本，并且顺手把“当前仓库已经完成到哪一步”也重写成和这个新目标一致。