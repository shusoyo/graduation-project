```bash
cd /workspace

repo sync

repo start [name] rel4_kernel

cd rel4_kernel

git remote add fork [link]
rel4-integral.git

git remote -v
```

日常开发：

```bash
cd /workspace/rel4_kernel

git status

# 修改代码

# 本地测试
cargo xtask run -p spike
```

提交：

```bash
cd /workspace/rel4_kernel

git add kernel sel4_cspace sel4_common
git commit -m "fix: 说明"
git push fork HEAD
```

推上 github：

1. 打开 fork 的 `rel4-integral`
2. 点 `Compare & pull request`
3. 目标仓库选 `rel4team2/rel4-integral`
4. 提交 PR
